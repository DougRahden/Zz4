# === LOAD INPUT FILE ===

# Updated to absolute D: path
input_filename = 'D:/aa030/input/aa-counterintel-input.xlsx'

df = pd.read_excel(input_filename, engine='openpyxl')
print(f"Loaded {len(df)} authors from Excel input.")
df.head()
